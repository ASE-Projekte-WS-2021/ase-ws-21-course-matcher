<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CM" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/gray600</item>
        <item name="colorPrimaryVariant">@color/gray700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/gray500</item>
        <item name="colorSecondaryVariant">@color/gray600</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/white</item>

        <!-- Typography -->
        <item name="android:fontFamily">@font/readexpro</item>
        <item name="android:textColor">@color/gray700</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">normal</item>


    </style>

    <style name="Theme.CM.Fullscreen" parent="Theme.CM">
        <item name="android:actionBarStyle">@style/Widget.Theme.CM.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.CM.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/gray600</item>
        <item name="fullscreenTextColor">@color/white</item>
    </style>
</resources>